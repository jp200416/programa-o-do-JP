# tabuada

try:
    n = int(input())
    m = int(input())
    if True:
        i = 1
        if i <= 10:
            r = n * (m * (i / m))
            print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
            i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
                i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
            i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
                i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
                i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
                i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
                i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
                i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
                i = i + 1
            if i <= 10:
                r = n * (m * (i / m))
                print(n, ord('x'), int(m * (i / m)), ord('='), int(r))
                i = i + 1
except ValueError:
    if True:
        print(*[ord(c) for c in 'Erro de conversao de numero.'])